{
  "name": "pureokrs",
  "version": "0.0.0",
  "engines": {
    "node": "16.x"
  },
  "scripts": {
    "heroku-prebuild": "cd server && npm i && cd .. && cd client && npm i --save-dev && npm i"
  },
  "private": true,
  "dependencies": {
  },
  "devDependencies": {
  }
}
