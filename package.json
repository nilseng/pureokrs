{
  "name": "pureokrs",
  "version": "0.0.0",
  "engines": {
    "node": "16.x"
  },
  "scripts": {
    "heroku-prebuild": "cd client && npm i && npm i --save-dev && cd .. && cd server && npm i"
  },
  "private": true,
  "dependencies": {
  },
  "devDependencies": {
  }
}
